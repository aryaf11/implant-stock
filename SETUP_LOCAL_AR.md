# مخزون الزرعات — قاعدة محلية (بدون Firebase)

البيانات تُحفظ على **هذا الجهاز/المتصفح** (Hive). لا حاجة لـ Firebase ولا إنترنت بعد التشغيل.

## الحسابات

| المستخدم | كلمة المرور |
|----------|-------------|
| `admin` | `1387raghad` |
| `branch1` | `branch1@123` |
| `branch2` | `branch2@123` |
| `branch3` | `branch3@123` |
| `branch4` | `branch4@123` |

## تشغيل

```bash
cd implant_stock
flutter pub get
flutter run -d edge
```

> إذا فتح الأدمن والفروع من **أجهزة مختلفة**، كل جهاز له مخزون منفصل. للمزامنة بين الأجهزة يلزم لاحقاً خادم (غير Firebase).
